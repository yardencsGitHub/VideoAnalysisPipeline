%% Preprocess pipeline
% Requirements:
% 1. A list of FS files as a cell array file_list in a file with the
%   same name
% 2. A source folder that holds those files (on a backup disk probably)
% 3. A folder for the renamed videos (Will also contain gifs and mat files for annotation)

% The script will 